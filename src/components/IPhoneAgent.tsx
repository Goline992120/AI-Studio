import React from 'react';
import { IphoneAssistantWidget } from './IphoneAssistantWidget';

export const IPhoneAgent: React.FC<any> = (props) => {
  return <IphoneAssistantWidget {...props} />;
};

export default IPhoneAgent;
